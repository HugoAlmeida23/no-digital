{
  "name": "no-digital",
  "version": "2.0.0",
  "description": "Nó Digital — Interactive wedding website landing page",
  "private": true,
  "scripts": {
    "dev": "npx serve . -l 3000 --no-clipboard",
    "build": "echo 'Static site — no build needed. Deploy index.html, styles.css, main.js, and public/ as-is.'",
    "preview": "npx serve . -l 4000"
  }
}
